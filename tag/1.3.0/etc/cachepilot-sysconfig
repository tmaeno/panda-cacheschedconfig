# Define cache areas
CACHEBIN=/opt/cacheschedconfig/bin
SVRPATH=/data/atlpan/srv
CACHEPATH=$SVRPATH/var/cache/pandaserver/pilot

# We need python 2.5
alias python=python2.5

# Add ourselves to the path
PATH=$CACHEBIN:$PATH

# Set HOME to /tmp to prevent svn from complaining
HOME=/tmp
